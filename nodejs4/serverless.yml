service: aws-nodejs4 # NOTE: update this with your service name

provider:
  name: aws
  runtime: nodejs4.3
  stage: dev
  region: eu-west-3
functions:
  hello:
    handler: handler.hello
    events:
      - http:
          path: /
          method: get
