service: aws-python3 # NOTE: update this with your service name

provider:
  name: aws
  runtime: python3.6
  stage: dev
  region: eu-west-3
functions:
  hello:
    handler: handler.hello
    events:
      - http:
          path: /
          method: get